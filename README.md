Independently designed and built an automated ETL orchestration pipeline in Apache Airflow, replacing a manual single-script process with 3 dependency-linked workflows that auto-trigger via event-based scheduling, processing 5,000+ records with zero hardcoded configuration — adopted as the technical blueprint for an upcoming billing-system initiative.

1. A heavier dataset (5,000 employee records) run through a real ETL
2. Auto-scheduling — no manual triggering
3. Three dependent ETL workflows
4. No hardcoded paths or business values anywhere in the code
